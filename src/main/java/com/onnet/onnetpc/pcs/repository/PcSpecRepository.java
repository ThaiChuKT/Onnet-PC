package com.onnet.onnetpc.pcs.repository;

import com.onnet.onnetpc.pcs.PcSpec;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PcSpecRepository extends JpaRepository<PcSpec, Long> {
}
